/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      "upload.wikimedia.org",
      "via.placeholder.com",
       "images.unsplash.com",
       "res.cloudinary.com",
     
    ],
  },
};

export default nextConfig;
