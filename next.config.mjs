/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      "upload.wikimedia.org",
      "via.placeholder.com",
       "images.unsplash.com",
       "res.cloudinary.com",
       'localhost', '127.0.0.1',
     
    ],
  },
};

export default nextConfig;
